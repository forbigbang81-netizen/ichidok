     1→     1→"use client";
     2→     2→
     3→     3→import { useCallback, useEffect, useRef, useState } from "react";
     4→     4→import {
     5→     5→  ArrowLeft,
     6→     6→  Captions,
     7→     7→  Loader2,
     8→     8→  Maximize,
     9→     9→  Minimize,
    10→    10→  Pause,
    11→    11→  Play,
    12→    12→  RotateCcw,
    13→    13→  RotateCw,
    14→    14→  Settings,
    15→    15→  Volume2,
    16→    16→  VolumeX,
    17→    17→} from "lucide-react";
    18→    18→import { fetchVideoImport, type VideoImport } from "@/lib/api/client";
    19→    19→import { useApp } from "@/store/app";
    20→    20→import { cn } from "@/lib/utils";
    21→    21→
    22→    22→interface SubtitleCue {
    23→    23→  start: number;
    24→    24→  end: number;
    25→    25→  text: string;
    26→    26→}
    27→    27→
    28→    28→interface VideoPlayerProps {
    29→    29→  malId: number;
    30→    30→  episode: number;
    31→    31→  audioMode: "SUB" | "DUB";
    32→    32→  poster?: string;
    33→    33→  title?: string;
    34→    34→  subtitle?: string;
    35→    35→  resumePosition?: number | null;
    36→    36→  onProgress?: (position: number, duration: number) => void;
    37→    37→  onEnded?: () => void;
    38→    38→  onBack?: () => void;
    39→    39→}
    40→    40→
    41→    41→const HIDE_CONTROLS_MS = 10000;
    42→    42→const SPEEDS = [0.5, 0.75, 1, 1.25, 1.5, 2];
    43→    43→const QUALITIES = ["Auto", "1080p", "720p", "480p", "360p"];
    44→    44→
    45→    45→function timeToSeconds(t: string): number {
    46→    46→  const m = t.match(/(\d{1,2}):(\d{2}):(\d{2})[.,](\d{1,3})/);
    47→    47→  if (!m) {
    48→    48→    const m2 = t.match(/(\d{1,2}):(\d{2})[.,](\d{1,3})/);
    49→    49→    if (!m2) return 0;
    50→    50→    return +m2[1] * 60 + +m2[2] + Number(m2[3]) / 1000;
    51→    51→  }
    52→    52→  return (
    53→    53→    +m[1] * 3600 + +m[2] * 60 + +m[3] + Number(m[4]) / Math.pow(10, m[4].length)
    54→    54→  );
    55→    55→}
    56→    56→
    57→    57→function parseVtt(text: string): SubtitleCue[] {
    58→    58→  const normalized = text.replace(/\r/g, "");
    59→    59→  const blocks = normalized.split(/\n\s*\n/);
    60→    60→  const cues: SubtitleCue[] = [];
    61→    61→  for (const block of blocks) {
    62→    62→    const lines = block.split("\n").filter((l) => l.trim() !== "");
    63→    63→    if (lines.length === 0) continue;
    64→    64→    if (lines[0].startsWith("WEBVTT")) continue;
    65→    65→    let timingLine = lines[0];
    66→    66→    let textStart = 1;
    67→    67→    if (!timingLine.includes("-->")) {
    68→    68→      if (lines.length < 2) continue;
    69→    69→      timingLine = lines[1];
    70→    70→      textStart = 2;
    71→    71→    }
    72→    72→    const m = timingLine.match(
    73→    73→      /([\d:.,]+)\s*-->\s*([\d:.,]+)/,
    74→    74→    );
    75→    75→    if (!m) continue;
    76→    76→    const start = timeToSeconds(m[1]);
    77→    77→    const end = timeToSeconds(m[2]);
    78→    78→    const cueText = lines.slice(textStart).join("\n").replace(/<[^>]+>/g, "");
    79→    79→    if (cueText.trim()) cues.push({ start, end, text: cueText });
    80→    80→  }
    81→    81→  return cues.sort((a, b) => a.start - b.start);
    82→    82→}
    83→    83→
    84→    84→function fmtTime(s: number): string {
    85→    85→  if (!Number.isFinite(s) || s < 0) s = 0;
    86→    86→  const h = Math.floor(s / 3600);
    87→    87→  const m = Math.floor((s % 3600) / 60);
    88→    88→  const sec = Math.floor(s % 60);
    89→    89→  if (h > 0)
    90→    90→    return `${h}:${String(m).padStart(2, "0")}:${String(sec).padStart(2, "0")}`;
    91→    91→  return `${m}:${String(sec).padStart(2, "0")}`;
    92→    92→}
    93→    93→
    94→    94→interface AudioTrackInfo {
    95→    95→  id: number;
    96→    96→  label: string;
    97→    97→  language?: string;
    98→    98→  enabled: boolean;
    99→    99→}
   100→   100→
   101→   101→// Minimal types for the non-standard HTMLMediaElement.audioTracks API.
   102→   102→interface AudioTrackLike {
   103→   103→  label: string;
   104→   104→  language: string;
   105→   105→  enabled: boolean;
   106→   106→}
   107→   107→interface AudioTrackListLike {
   108→   108→  length: number;
   109→   109→  [index: number]: AudioTrackLike;
   110→   110→}
   111→   111→interface VideoElementWithAudioTracks extends HTMLVideoElement {
   112→   112→  audioTracks?: AudioTrackListLike;
   113→   113→}
   114→   114→
   115→   115→export function VideoPlayer({
   116→   116→  malId,
   117→   117→  episode,
   118→   118→  audioMode,
   119→   119→  poster,
   120→   120→  title,
   121→   121→  subtitle,
   122→   122→  resumePosition,
   123→   123→  onProgress,
   124→   124→  onEnded,
   125→   125→  onBack,
   126→   126→}: VideoPlayerProps) {
   127→   127→  const videoRef = useRef<HTMLVideoElement>(null);
   128→   128→  const containerRef = useRef<HTMLDivElement>(null);
   129→   129→  const hideTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
   130→   130→  const seekingRef = useRef(false);
   131→   131→  const lastProgressEmitRef = useRef(0);
   132→   132→  const resumeAppliedRef = useRef(false);
   133→   133→
   134→   134→  const { setAudioMode, setQuality, setSpeed, selectedQuality, selectedSpeed, toggleFullscreen, fullscreenPlayer } = useApp();
   135→   135→
   136→   136→  const [importInfo, setImportInfo] = useState<VideoImport | null>(null);
   137→   137→  const [loading, setLoading] = useState(true);
   138→   138→  const [error, setError] = useState<string | null>(null);
   139→   139→
   140→   140→  const [isPlaying, setIsPlaying] = useState(false);
   141→   141→  const [currentTime, setCurrentTime] = useState(0);
   142→   142→  const [duration, setDuration] = useState(0);
   143→   143→  const [buffered, setBuffered] = useState(0);
   144→   144→  const [controlsVisible, setControlsVisible] = useState(true);
   145→   145→  const [showSettings, setShowSettings] = useState(false);
   146→   146→  const [showSubtitles, setShowSubtitles] = useState(true);
   147→   147→  const [cues, setCues] = useState<SubtitleCue[]>([]);
   148→   148→  const [activeCue, setActiveCue] = useState("");
   149→   149→  const [muted, setMuted] = useState(false);
   150→   150→  const [audioTracks, setAudioTracks] = useState<AudioTrackInfo[]>([]);
   151→   151→  const [activeAudioTrack, setActiveAudioTrack] = useState<number>(0);
   152→   152→  const [isFullscreen, setIsFullscreen] = useState(false);
   153→   153→  const [subtitleSource, setSubtitleSource] = useState<string>("");
   154→   154→
   155→   155→  // ----- Auto-hide controls -----
   156→   156→  const keepControlsAlive = useCallback(() => {
   157→   157→    setControlsVisible(true);
   158→   158→    if (hideTimerRef.current) clearTimeout(hideTimerRef.current);
   159→   159→    hideTimerRef.current = setTimeout(() => {
   160→   160→      setControlsVisible(false);
   161→   161→      setShowSettings(false);
   162→   162→    }, HIDE_CONTROLS_MS);
   163→   163→  }, []);
   164→   164→
   165→   165→  useEffect(() => {
   166→   166→    keepControlsAlive();
   167→   167→    return () => {
   168→   168→      if (hideTimerRef.current) clearTimeout(hideTimerRef.current);
   169→   169→    };
   170→   170→  }, [keepControlsAlive]);
   171→   171→
   172→   172→  // ----- Fetch video import -----
   173→   173→  useEffect(() => {
   174→   174→    let cancelled = false;
   175→   175→    setLoading(true);
   176→   176→    setError(null);
   177→   177→    setImportInfo(null);
   178→   178→    setCues([]);
   179→   179→    setActiveCue("");
   180→   180→    resumeAppliedRef.current = false;
   181→   181→    fetchVideoImport(malId, episode, audioMode.toLowerCase() as "sub" | "dub")
   182→   182→      .then((info) => {
   183→   183→        if (cancelled) return;
   184→   184→        setImportInfo(info);
   185→   185→        setLoading(false);
   186→   186→        if (!info || !info.url) {
   187→   187→          setError(
   188→   188→            "No stream available for this episode yet. Try another episode or audio mode.",
   189→   189→          );
   190→   190→        }
   191→   191→      })
   192→   192→      .catch((e) => {
   193→   193→        if (cancelled) return;
   194→   194→        setError(String(e?.message ?? e));
   195→   195→        setLoading(false);
   196→   196→      });
   197→   197→    return () => {
   198→   198→      cancelled = true;
   199→   199→    };
   200→   200→  }, [malId, episode, audioMode]);
   201→   201→
   202→   202→  // ----- Load subtitles (VTT) -----
   203→   203→  useEffect(() => {
   204→   204→    const subUrl = importInfo?.subtitleUrl ?? subtitle ?? null;
   205→   205→    if (!subUrl) {
   206→   206→      setCues([]);
   207→   207→      return;
   208→   208→    }
   209→   209→    let cancelled = false;
   210→   210→    fetch(subUrl)
   211→   211→      .then((r) => {
   212→   212→        if (!r.ok) return Promise.reject(r.status);
   213→   213→        // Track subtitle source for UI feedback (e.g. "OpenSubtitles" vs "Episode Info")
   214→   214→        const src = r.headers.get("x-subtitle-source");
   215→   215→        if (src) setSubtitleSource(src);
   216→   216→        return r.text();
   217→   217→      })
   218→   218→      .then((text) => {
   219→   219→        if (cancelled) return;
   220→   220→        try {
   221→   221→          setCues(parseVtt(text));
   222→   222→        } catch {
   223→   223→          setCues([]);
   224→   224→        }
   225→   225→      })
   226→   226→      .catch(() => {
   227→   227→        if (!cancelled) setCues([]);
   228→   228→      });
   229→   229→    return () => {
   230→   230→      cancelled = true;
   231→   231→    };
   232→   232→  }, [importInfo?.subtitleUrl, subtitle]);
   233→   233→
   234→   234→  // ----- Wire up video element events -----
   235→   235→  useEffect(() => {
   236→   236→    const v = videoRef.current;
   237→   237→    if (!v) return;
   238→   238→
   239→   239→    const onPlay = () => setIsPlaying(true);
   240→   240→    const onPause = () => setIsPlaying(false);
   241→   241→    const onTime = () => {
   242→   242→      if (seekingRef.current) return;
   243→   243→      setCurrentTime(v.currentTime);
   244→   244→      // Update active cue
   245→   245→      const cue = cues.find((c) => v.currentTime >= c.start && v.currentTime <= c.end);
   246→   246→      setActiveCue(cue ? cue.text : "");
   247→   247→      // Throttled progress callback (every 5s)
   248→   248→      const now = Date.now();
   249→   249→      if (onProgress && now - lastProgressEmitRef.current > 5000) {
   250→   250→        lastProgressEmitRef.current = now;
   251→   251→        onProgress(v.currentTime, v.duration || 0);
   252→   252→      }
   253→   253→    };
   254→   254→    const onLoadedMeta = () => {
   255→   255→      setDuration(v.duration || 0);
   256→   256→      // Resume position
   257→   257→      if (!resumeAppliedRef.current && resumePosition && resumePosition > 5) {
   258→   258→        try {
   259→   259→          v.currentTime = Math.min(resumePosition, (v.duration || resumePosition) - 5);
   260→   260→        } catch {}
   261→   261→      }
   262→   262→      resumeAppliedRef.current = true;
   263→   263→      // Audio tracks
   264→   264→      const at = (v as VideoElementWithAudioTracks).audioTracks;
   265→   265→      if (at && at.length > 1) {
   266→   266→        const tracks: AudioTrackInfo[] = [];
   267→   267→        for (let i = 0; i < at.length; i++) {
   268→   268→          const t = at[i];
   269→   269→          tracks.push({
   270→   270→            id: i,
   271→   271→            label: t.label || t.language || `Audio ${i + 1}`,
   272→   272→            language: t.language || undefined,
   273→   273→            enabled: t.enabled,
   274→   274→          });
   275→   275→        }
   276→   276→        setAudioTracks(tracks);
   277→   277→        const enabledIdx = tracks.findIndex((t) => t.enabled);
   278→   278→        setActiveAudioTrack(enabledIdx >= 0 ? enabledIdx : 0);
   279→   279→      } else {
   280→   280→        setAudioTracks([]);
   281→   281→      }
   282→   282→    };
   283→   283→    const onProgressBuf = () => {
   284→   284→      try {
   285→   285→        if (v.buffered.length > 0) {
   286→   286→          setBuffered(v.buffered.end(v.buffered.length - 1));
   287→   287→        }
   288→   288→      } catch {}
   289→   289→    };
   290→   290→    const onEndedEv = () => {
   291→   291→      if (onProgress && v.duration) onProgress(v.duration, v.duration);
   292→   292→      onEnded?.();
   293→   293→    };
   294→   294→    const onErr = () => {
   295→   295→      // Don't override the initial "no stream" error.
   296→   296→      setError((prev) => prev ?? "Video failed to load. The source may be unavailable.");
   297→   297→    };
   298→   298→
   299→   299→    v.addEventListener("play", onPlay);
   300→   300→    v.addEventListener("pause", onPause);
   301→   301→    v.addEventListener("timeupdate", onTime);
   302→   302→    v.addEventListener("loadedmetadata", onLoadedMeta);
   303→   303→    v.addEventListener("progress", onProgressBuf);
   304→   304→    v.addEventListener("ended", onEndedEv);
   305→   305→    v.addEventListener("error", onErr);
   306→   306→    return () => {
   307→   307→      v.removeEventListener("play", onPlay);
   308→   308→      v.removeEventListener("pause", onPause);
   309→   309→      v.removeEventListener("timeupdate", onTime);
   310→   310→      v.removeEventListener("loadedmetadata", onLoadedMeta);
   311→   311→      v.removeEventListener("progress", onProgressBuf);
   312→   312→      v.removeEventListener("ended", onEndedEv);
   313→   313→      v.removeEventListener("error", onErr);
   314→   314→    };
   315→   315→  }, [cues, onProgress, onEnded, resumePosition]);
   316→   316→
   317→   317→  // ----- Apply playback rate -----
   318→   318→  useEffect(() => {
   319→   319→    const v = videoRef.current;
   320→   320→    if (!v) return;
   321→   321→    v.playbackRate = selectedSpeed;
   322→   322→  }, [selectedSpeed]);
   323→   323→
   324→   324→  // ----- Fullscreen change listener -----
   325→   325→  useEffect(() => {
   326→   326→    const onFs = () => {
   327→   327→      const fs = !!document.fullscreenElement;
   328→   328→      setIsFullscreen(fs);
   329→   329→      if (fullscreenPlayer !== fs) toggleFullscreen();
   330→   330→    };
   331→   331→    document.addEventListener("fullscreenchange", onFs);
   332→   332→    return () => document.removeEventListener("fullscreenchange", onFs);
   333→   333→  }, [fullscreenPlayer, toggleFullscreen]);
   334→   334→
   335→   335→  // ----- Controls -----
   336→   336→  const togglePlay = useCallback(() => {
   337→   337→    const v = videoRef.current;
   338→   338→    if (!v) return;
   339→   339→    keepControlsAlive();
   340→   340→    if (v.paused) v.play().catch(() => {});
   341→   341→    else v.pause();
   342→   342→  }, [keepControlsAlive]);
   343→   343→
   344→   344→  const skip = useCallback(
   345→   345→    (delta: number) => {
   346→   346→      const v = videoRef.current;
   347→   347→      if (!v) return;
   348→   348→      v.currentTime = Math.max(0, Math.min((v.duration || 0), v.currentTime + delta));
   349→   349→      keepControlsAlive();
   350→   350→    },
   351→   351→    [keepControlsAlive],
   352→   352→  );
   353→   353→
   354→   354→  const seekTo = useCallback(
   355→   355→    (t: number) => {
   356→   356→      const v = videoRef.current;
   357→   357→      if (!v) return;
   358→   358→      v.currentTime = Math.max(0, Math.min((v.duration || 0), t));
   359→   359→      setCurrentTime(v.currentTime);
   360→   360→      keepControlsAlive();
   361→   361→    },
   362→   362→    [keepControlsAlive],
   363→   363→  );
   364→   364→
   365→   365→  const toggleMute = useCallback(() => {
   366→   366→    const v = videoRef.current;
   367→   367→    if (!v) return;
   368→   368→    v.muted = !v.muted;
   369→   369→    setMuted(v.muted);
   370→   370→    keepControlsAlive();
   371→   371→  }, [keepControlsAlive]);
   372→   372→
   373→   373→  const toggleFullscreenFn = useCallback(() => {
   374→   374→    const el = containerRef.current;
   375→   375→    if (!el) return;
   376→   376→    if (document.fullscreenElement) {
   377→   377→      document.exitFullscreen().catch(() => {});
   378→   378→    } else {
   379→   379→      el.requestFullscreen().catch(() => {});
   380→   380→    }
   381→   381→    keepControlsAlive();
   382→   382→  }, [keepControlsAlive]);
   383→   383→
   384→   384→  const handleVideoTap = useCallback(() => {
   385→   385→    // Tap video toggles UI (does NOT pause).
   386→   386→    setControlsVisible((prev) => {
   387→   387→      const next = !prev;
   388→   388→      if (next) {
   389→   389→        if (hideTimerRef.current) clearTimeout(hideTimerRef.current);
   390→   390→        hideTimerRef.current = setTimeout(() => {
   391→   391→          setControlsVisible(false);
   392→   392→          setShowSettings(false);
   393→   393→        }, HIDE_CONTROLS_MS);
   394→   394→      } else {
   395→   395→        if (hideTimerRef.current) clearTimeout(hideTimerRef.current);
   396→   396→      }
   397→   397→      return next;
   398→   398→    });
   399→   399→  }, []);
   400→   400→
   401→   401→  const switchAudioTrack = useCallback(
   402→   402→    (id: number) => {
   403→   403→      const v = videoRef.current as VideoElementWithAudioTracks | null;
   404→   404→      if (!v || !v.audioTracks) return;
   405→   405→      for (let i = 0; i < v.audioTracks.length; i++) {
   406→   406→        v.audioTracks[i].enabled = i === id;
   407→   407→      }
   408→   408→      setActiveAudioTrack(id);
   409→   409→      keepControlsAlive();
   410→   410→    },
   411→   411→    [keepControlsAlive],
   412→   412→  );
   413→   413→
   414→   414→  const switchAudioMode = useCallback(
   415→   415→    (mode: "SUB" | "DUB") => {
   416→   416→      setAudioMode(mode);
   417→   417→      keepControlsAlive();
   418→   418→    },
   419→   419→    [setAudioMode, keepControlsAlive],
   420→   420→  );
   421→   421→
   422→   422→  const onSeekScrub = (val: number[]) => {
   423→   423→    seekingRef.current = true;
   424→   424→    setCurrentTime(val[0]);
   425→   425→  };
   426→   426→  const onSeekCommit = (val: number[]) => {
   427→   427→    seekingRef.current = false;
   428→   428→    seekTo(val[0]);
   429→   429→  };
   430→   430→
   431→   431→  // ----- Render -----
   432→   432→  const isYoutube = !!importInfo?.isYoutube && !!importInfo?.url;
   433→   433→  const videoUrl = importInfo?.url ?? null;
   434→   434→  const posterUrl = poster ?? "";
   435→   435→
   436→   436→  // YouTube iframe branch
   437→   437→  if (isYoutube && videoUrl) {
   438→   438→    const ytId = (() => {
   439→   439→      try {
   440→   440→        const u = new URL(videoUrl);
   441→   441→        if (u.hostname.includes("youtube.com")) {
   442→   442→          return u.pathname.startsWith("/embed/")
   443→   443→            ? u.pathname.slice("/embed/".length)
   444→   444→            : u.searchParams.get("v") ?? "";
   445→   445→        }
   446→   446→        if (u.hostname.includes("youtu.be")) return u.pathname.slice(1);
   447→   447→      } catch {}
   448→   448→      return "";
   449→   449→    })();
   450→   450→    const embedSrc = ytId
   451→   451→      ? `https://www.youtube.com/embed/${ytId}?autoplay=1&rel=0&modestbranding=1`
   452→   452→      : videoUrl;
   453→   453→
   454→   454→    return (
   455→   455→      <div
   456→   456→        ref={containerRef}
   457→   457→        className="relative w-full overflow-hidden bg-black aspect-video"
   458→   458→      >
   459→   459→        <iframe
   460→   460→          src={embedSrc}
   461→   461→          title={title ?? "Video player"}
   462→   462→          className="absolute inset-0 h-full w-full"
   463→   463→          allow="autoplay; fullscreen; picture-in-picture; encrypted-media"
   464→   464→          allowFullScreen
   465→   465→        />
   466→   466→        {onBack && (
   467→   467→          <button
   468→   468→            type="button"
   469→   469→            onClick={onBack}
   470→   470→            className="absolute top-3 left-3 z-10 grid h-9 w-9 place-items-center rounded-full bg-black/60 text-white hover:bg-black/80"
   471→   471→            aria-label="Back"
   472→   472→          >
   473→   473→            <ArrowLeft className="h-5 w-5" />
   474→   474→          </button>
   475→   475→        )}
   476→   476→        <div className="pointer-events-none absolute bottom-3 left-3 z-10 rounded-md bg-black/60 px-2 py-1 text-[10px] font-semibold uppercase tracking-wide text-white">
   477→   477→          Trailer · YouTube
   478→   478→        </div>
   479→   479→      </div>
   480→   480→    );
   481→   481→  }
   482→   482→
   483→   483→  const progressPct =
   484→   484→    duration > 0 ? Math.min(100, (currentTime / duration) * 100) : 0;
   485→   485→  const bufferedPct =
   486→   486→    duration > 0 ? Math.min(100, (buffered / duration) * 100) : 0;
   487→   487→
   488→   488→  return (
   489→   489→    <div
   490→   490→      ref={containerRef}
   491→   491→      className={cn(
   492→   492→        "relative w-full overflow-hidden bg-black aspect-video select-none",
   493→   493→        isFullscreen && "rounded-none",
   494→   494→      )}
   495→   495→      onMouseMove={keepControlsAlive}
   496→   496→      onMouseLeave={() => {
   497→   497→        if (hideTimerRef.current) clearTimeout(hideTimerRef.current);
   498→   498→        hideTimerRef.current = setTimeout(() => {
   499→   499→          setControlsVisible(false);
   500→   500→          setShowSettings(false);
   501→   501→        }, HIDE_CONTROLS_MS / 2);
   502→   502→      }}
   503→   503→    >
   504→   504→      {/* Video element — NO crossOrigin (breaks archive.org CDN).
   505→   505→          Tap-to-toggle-UI is handled by a dedicated click-catcher below
   506→   506→          (the <video> element's onClick is unreliable on mobile). */}
   507→   507→      {videoUrl ? (
   508→   508→        <video
   509→   509→          ref={videoRef}
   510→   510→          src={videoUrl}
   511→   511→          poster={posterUrl}
   512→   512→          playsInline
   513→   513→          preload="metadata"
   514→   514→          className="absolute inset-0 h-full w-full bg-black"
   515→   515→        />
   516→   516→      ) : (
   517→   517→        <div className="absolute inset-0 grid place-items-center bg-black">
   518→   518→          {posterUrl && (
   519→   519→            <img
   520→   520→              src={posterUrl}
   521→   521→              alt={title ?? "poster"}
   522→   522→              className="absolute inset-0 h-full w-full object-cover opacity-40"
   523→   523→            />
   524→   524→          )}
   525→   525→        </div>
   526→   526→      )}
   527→   527→
   528→   528→      {/* Always-present tap catcher — toggles the controls UI on click.
   529→   529→          Sits ABOVE the video (z-10) but BELOW the controls (z-20/z-30).
   530→   530→          The top/bottom bars become pointer-events-none when hidden, so
   531→   531→          taps fall through to this layer; the center overlay's buttons
   532→   532→          call e.stopPropagation() so they don't trigger this handler. */}
   533→   533→      {!loading && !error && videoUrl && (
   534→   534→        <div
   535→   535→          className="absolute inset-0 z-10"
   536→   536→          onClick={handleVideoTap}
   537→   537→        />
   538→   538→      )}
   539→   539→
   540→   540→      {/* Loading overlay */}
   541→   541→      {loading && (
   542→   542→        <div className="absolute inset-0 grid place-items-center bg-black/70">
   543→   543→          <div className="flex flex-col items-center gap-3 text-white">
   544→   544→            <Loader2 className="h-10 w-10 animate-spin text-yellow-400" />
   545→   545→            <p className="text-xs tracking-wide text-white/70">
   546→   546→              Resolving stream…
   547→   547→            </p>
   548→   548→          </div>
   549→   549→        </div>
   550→   550→      )}
   551→   551→
   552→   552→      {/* Error overlay */}
   553→   553→      {!loading && error && (
   554→   554→        <div className="absolute inset-0 grid place-items-center bg-black/80 p-6">
   555→   555→          <div className="flex max-w-xs flex-col items-center gap-3 text-center text-white">
   556→   556→            <p className="text-sm">{error}</p>
   557→   557→          </div>
   558→   558→        </div>
   559→   559→      )}
   560→   560→
   561→   561→      {/* Custom subtitle overlay — no black box, text-base, bottom-4, WebkitTextStroke outline */}
   562→   562→      {showSubtitles && activeCue && (
   563→   563→        <div className="pointer-events-none absolute inset-x-0 bottom-16 z-20 flex justify-center px-4 sm:bottom-20">
   564→   564→          <span
   565→   565→            className="max-w-[90%] text-center text-base font-semibold leading-snug text-white"
   566→   566→            style={{
   567→   567→              WebkitTextStroke: "1.5px rgba(0,0,0,0.85)",
   568→   568→              paintOrder: "stroke fill",
   569→   569→              textShadow: "0 2px 4px rgba(0,0,0,0.7)",
   570→   570→            }}
   571→   571→          >
   572→   572→            {activeCue.split("\n").map((line, i) => (
   573→   573→              <span key={i} className="block">
   574→   574→                {line}
   575→   575→              </span>
   576→   576→            ))}
   577→   577→          </span>
   578→   578→        </div>
   579→   579→      )}
   580→   580→
   581→   581→      {/* Subtitle "unavailable" hint — shows briefly when CC is on but no cues */}
   582→   582→      {showSubtitles && !activeCue && cues.length === 0 && !loading && videoUrl && (
   583→   583→        <div className="pointer-events-none absolute inset-x-0 bottom-16 z-20 flex justify-center px-4 sm:bottom-20">
   584→   584→          <span
   585→   585→            className="rounded bg-black/60 px-3 py-1 text-center text-xs font-medium text-white/80"
   586→   586→            style={{ textShadow: "0 1px 2px rgba(0,0,0,0.7)" }}
   587→   587→          >
   588→   588→            字幕なし · No subtitles available for this episode
   589→   589→          </span>
   590→   590→        </div>
   591→   591→      )}
   592→   592→
   593→   593→      {/* Top bar */}
   594→   594→      <div
   595→   595→        className={cn(
   596→   596→          "absolute inset-x-0 top-0 z-30 flex items-center gap-2 bg-gradient-to-b from-black/80 to-transparent p-3 transition-opacity duration-300",
   597→   597→          controlsVisible ? "opacity-100" : "opacity-0 pointer-events-none",
   598→   598→        )}
   599→   599→      >
   600→   600→        {onBack && (
   601→   601→          <button
   602→   602→            type="button"
   603→   603→            onClick={(e) => {
   604→   604→              e.stopPropagation();
   605→   605→              onBack();
   606→   606→            }}
   607→   607→            className="grid h-9 w-9 place-items-center rounded-full bg-black/50 text-white hover:bg-black/70"
   608→   608→            aria-label="Back"
   609→   609→          >
   610→   610→            <ArrowLeft className="h-5 w-5" />
   611→   611→          </button>
   612→   612→        )}
   613→   613→        <div className="min-w-0 flex-1">
   614→   614→          <p className="truncate text-sm font-semibold text-white">
   615→   615→            {title ?? "Now Playing"}
   616→   616→          </p>
   617→   617→          {importInfo && (
   618→   618→            <p className="text-[11px] text-white/60">
   619→   619→              Episode {episode} · {audioMode} · {importInfo.quality}
   620→   620→            </p>
   621→   621→          )}
   622→   622→        </div>
   623→   623→        {(importInfo?.hasSub || importInfo?.hasDub) && (
   624→   624→          <div className="flex overflow-hidden rounded-md border border-white/20 text-xs font-bold">
   625→   625→            <button
   626→   626→              type="button"
   627→   627→              onClick={(e) => {
   628→   628→                e.stopPropagation();
   629→   629→                switchAudioMode("SUB");
   630→   630→              }}
   631→   631→              className={cn(
   632→   632→                "px-2 py-1 transition-colors",
   633→   633→                audioMode === "SUB"
   634→   634→                  ? "bg-yellow-400 text-black"
   635→   635→                  : "bg-black/40 text-white hover:bg-black/60",
   636→   636→              )}
   637→   637→            >
   638→   638→              SUB
   639→   639→            </button>
   640→   640→            <button
   641→   641→              type="button"
   642→   642→              onClick={(e) => {
   643→   643→                e.stopPropagation();
   644→   644→                switchAudioMode("DUB");
   645→   645→              }}
   646→   646→              className={cn(
   647→   647→                "px-2 py-1 transition-colors",
   648→   648→                audioMode === "DUB"
   649→   649→                  ? "bg-yellow-400 text-black"
   650→   650→                  : "bg-black/40 text-white hover:bg-black/60",
   651→   651→              )}
   652→   652→            >
   653→   653→              DUB
   654→   654→            </button>
   655→   655→          </div>
   656→   656→        )}
   657→   657→      </div>
   658→   658→
   659→   659→      {/* Center controls — only when controls visible.
   660→   660→          The wrapper is pointer-events-none so taps on empty center area
   661→   661→          fall through to the click-catcher (z-10) which toggles the UI.
   662→   662→          The buttons inside re-enable pointer-events-auto and call
   663→   663→          e.stopPropagation() so they don't trigger the toggle. */}
   664→   664→      {controlsVisible && !loading && !error && videoUrl && (
   665→   665→        <div className="pointer-events-none absolute inset-0 z-20 grid place-items-center">
   666→   666→          <div className="pointer-events-auto flex items-center gap-6">
   667→   667→            <button
   668→   668→              type="button"
   669→   669→              onClick={(e) => {
   670→   670→                e.stopPropagation();
   671→   671→                skip(-10);
   672→   672→              }}
   673→   673→              className="grid h-11 w-11 place-items-center rounded-full bg-black/50 text-white backdrop-blur hover:bg-black/70"
   674→   674→              aria-label="Back 10 seconds"
   675→   675→            >
   676→   676→              <RotateCcw className="h-5 w-5" />
   677→   677→            </button>
   678→   678→            <button
   679→   679→              type="button"
   680→   680→              onClick={(e) => {
   681→   681→                e.stopPropagation();
   682→   682→                togglePlay();
   683→   683→              }}
   684→   684→              className="grid h-16 w-16 place-items-center rounded-full bg-yellow-400 text-black shadow-lg shadow-yellow-500/30 transition hover:scale-105 hover:bg-yellow-300"
   685→   685→              aria-label={isPlaying ? "Pause" : "Play"}
   686→   686→            >
   687→   687→              {isPlaying ? (
   688→   688→                <Pause className="h-7 w-7" />
   689→   689→              ) : (
   690→   690→                <Play className="h-7 w-7 translate-x-0.5" />
   691→   691→              )}
   692→   692→            </button>
   693→   693→            <button
   694→   694→              type="button"
   695→   695→              onClick={(e) => {
   696→   696→                e.stopPropagation();
   697→   697→                skip(10);
   698→   698→              }}
   699→   699→              className="grid h-11 w-11 place-items-center rounded-full bg-black/50 text-white backdrop-blur hover:bg-black/70"
   700→   700→              aria-label="Forward 10 seconds"
   701→   701→            >
   702→   702→              <RotateCw className="h-5 w-5" />
   703→   703→            </button>
   704→   704→          </div>
   705→   705→        </div>
   706→   706→      )}
   707→   707→
   708→   708→      {/* Bottom controls */}
   709→   709→      <div
   710→   710→        className={cn(
   711→   711→          "absolute inset-x-0 bottom-0 z-30 bg-gradient-to-t from-black/85 to-transparent px-3 pb-3 pt-8 transition-opacity duration-300",
   712→   712→          controlsVisible ? "opacity-100" : "opacity-0 pointer-events-none",
   713→   713→        )}
   714→   714→        onMouseMove={keepControlsAlive}
   715→   715→        onClick={(e) => e.stopPropagation()}
   716→   716→      >
   717→   717→        {/* Progress bar (seekable) */}
   718→   718→        <div className="group relative mb-2 h-1.5 w-full cursor-pointer rounded-full bg-white/20">
   719→   719→          <div
   720→   720→            className="absolute left-0 top-0 h-full rounded-full bg-white/30"
   721→   721→            style={{ width: `${bufferedPct}%` }}
   722→   722→          />
   723→   723→          <div
   724→   724→            className="absolute left-0 top-0 h-full rounded-full bg-yellow-400"
   725→   725→            style={{ width: `${progressPct}%` }}
   726→   726→          />
   727→   727→          <input
   728→   728→            type="range"
   729→   729→            min={0}
   730→   730→            max={duration || 0}
   731→   731→            step={0.1}
   732→   732→            value={currentTime}
   733→   733→            onChange={(e) => onSeekScrub([Number(e.target.value)])}
   734→   734→            onPointerUp={(e) => onSeekCommit([Number((e.target as HTMLInputElement).value)])}
   735→   735→            onTouchEnd={(e) => onSeekCommit([Number((e.target as HTMLInputElement).value)])}
   736→   736→            className="absolute inset-0 h-full w-full cursor-pointer opacity-0"
   737→   737→            aria-label="Seek"
   738→   738→          />
   739→   739→          <div
   740→   740→            className="pointer-events-none absolute top-1/2 h-3 w-3 -translate-x-1/2 -translate-y-1/2 rounded-full bg-yellow-400 shadow"
   741→   741→            style={{ left: `${progressPct}%` }}
   742→   742→          />
   743→   743→        </div>
   744→   744→
   745→   745→        <div className="flex items-center gap-2 text-white">
   746→   746→          <button
   747→   747→            type="button"
   748→   748→            onClick={togglePlay}
   749→   749→            className="grid h-8 w-8 place-items-center rounded-full hover:bg-white/10"
   750→   750→            aria-label={isPlaying ? "Pause" : "Play"}
   751→   751→          >
   752→   752→            {isPlaying ? (
   753→   753→              <Pause className="h-4 w-4" />
   754→   754→            ) : (
   755→   755→              <Play className="h-4 w-4" />
   756→   756→            )}
   757→   757→          </button>
   758→   758→          <button
   759→   759→            type="button"
   760→   760→            onClick={toggleMute}
   761→   761→            className="grid h-8 w-8 place-items-center rounded-full hover:bg-white/10"
   762→   762→            aria-label="Mute"
   763→   763→          >
   764→   764→            {muted ? (
   765→   765→              <VolumeX className="h-4 w-4" />
   766→   766→            ) : (
   767→   767→              <Volume2 className="h-4 w-4" />
   768→   768→            )}
   769→   769→          </button>
   770→   770→          <span className="text-[11px] tabular-nums text-white/80">
   771→   771→            {fmtTime(currentTime)} / {fmtTime(duration)}
   772→   772→          </span>
   773→   773→
   774→   774→          <div className="ml-auto flex items-center gap-1">
   775→   775→            {/* CC toggle — show whenever the source claims subtitles, even
   776→   776→                before cues finish loading, so users can toggle it on/off. */}
   777→   777→            {(cues.length > 0 || importInfo?.hasSub || importInfo?.subtitleUrl) && (
   778→   778→              <button
   779→   779→                type="button"
   780→   780→                onClick={() => {
   781→   781→                  setShowSubtitles((s) => !s);
   782→   782→                  keepControlsAlive();
   783→   783→                }}
   784→   784→                className={cn(
   785→   785→                  "grid h-8 w-8 place-items-center rounded-full hover:bg-white/10",
   786→   786→                  showSubtitles ? "text-yellow-400" : "text-white/70",
   787→   787→                )}
   788→   788→                aria-label="Toggle subtitles"
   789→   789→                title={
   790→   790→                  cues.length === 0
   791→   791→                    ? "Subtitles unavailable for this episode"
   792→   792→                    : subtitleSource === "opensubtitles"
   793→   793→                      ? "Subtitles from OpenSubtitles (real dialogue)"
   794→   794→                      : subtitleSource.startsWith("jikan")
   795→   795→                        ? "Episode info (configure OpenSubtitles for dialogue subs)"
   796→   796→                        : "Toggle subtitles"
   797→   797→                }
   798→   798→              >
   799→   799→                <Captions className="h-4 w-4" />
   800→   800→              </button>
   801→   801→            )}
   802→   802→
   803→   803→            {/* Audio tracks (only when dual-audio detected) */}
   804→   804→            {audioTracks.length > 0 && (
   805→   805→              <AudioTracksMenu
   806→   806→                tracks={audioTracks}
   807→   807→                active={activeAudioTrack}
   808→   808→                onSelect={switchAudioTrack}
   809→   809→                onHover={keepControlsAlive}
   810→   810→              />
   811→   811→            )}
   812→   812→
   813→   813→            {/* Settings: speed + quality */}
   814→   814→            <SettingsMenu
   815→   815→              speed={selectedSpeed}
   816→   816→              quality={selectedQuality}
   817→   817→              onSpeed={(s) => {
   818→   818→                setSpeed(s);
   819→   819→                keepControlsAlive();
   820→   820→              }}
   821→   821→              onQuality={(q) => {
   822→   822→                setQuality(q);
   823→   823→                keepControlsAlive();
   824→   824→              }}
   825→   825→              onHover={keepControlsAlive}
   826→   826→              open={showSettings}
   827→   827→              onOpenChange={(o) => {
   828→   828→                setShowSettings(o);
   829→   829→                if (o) keepControlsAlive();
   830→   830→              }}
   831→   831→            />
   832→   832→
   833→   833→            <button
   834→   834→              type="button"
   835→   835→              onClick={toggleFullscreenFn}
   836→   836→              className="grid h-8 w-8 place-items-center rounded-full hover:bg-white/10"
   837→   837→              aria-label="Fullscreen"
   838→   838→            >
   839→   839→              {isFullscreen ? (
   840→   840→                <Minimize className="h-4 w-4" />
   841→   841→              ) : (
   842→   842→                <Maximize className="h-4 w-4" />
   843→   843→              )}
   844→   844→            </button>
   845→   845→          </div>
   846→   846→        </div>
   847→   847→      </div>
   848→   848→    </div>
   849→   849→  );
   850→   850→}
   851→   851→
   852→   852→interface AudioTracksMenuProps {
   853→   853→  tracks: AudioTrackInfo[];
   854→   854→  active: number;
   855→   855→  onSelect: (id: number) => void;
   856→   856→  onHover: () => void;
   857→   857→}
   858→   858→
   859→   859→function AudioTracksMenu({ tracks, active, onSelect, onHover }: AudioTracksMenuProps) {
   860→   860→  const [open, setOpen] = useState(false);
   861→   861→  return (
   862→   862→    <div
   863→   863→      className="relative"
   864→   864→      onMouseEnter={onHover}
   865→   865→    >
   866→   866→      <button
   867→   867→        type="button"
   868→   868→        onClick={() => setOpen((o) => !o)}
   869→   869→        className="grid h-8 w-8 place-items-center rounded-full text-[10px] font-bold hover:bg-white/10"
   870→   870→        aria-label="Audio tracks"
   871→   871→      >
   872→   872→        AUD
   873→   873→      </button>
   874→   874→      {open && (
   875→   875→        <div className="absolute bottom-10 right-0 w-40 overflow-hidden rounded-md border border-white/10 bg-[#1a1a1f] shadow-xl">
   876→   876→          <p className="px-3 py-2 text-[10px] font-semibold uppercase tracking-wide text-white/50">
   877→   877→            Audio Track
   878→   878→          </p>
   879→   879→          {tracks.map((t) => (
   880→   880→            <button
   881→   881→              key={t.id}
   882→   882→              type="button"
   883→   883→              onClick={() => {
   884→   884→                onSelect(t.id);
   885→   885→                setOpen(false);
   886→   886→              }}
   887→   887→              className={cn(
   888→   888→                "block w-full px-3 py-2 text-left text-xs hover:bg-white/5",
   889→   889→                t.id === active ? "text-yellow-400" : "text-white",
   890→   890→              )}
   891→   891→            >
   892→   892→              {t.label}
   893→   893→              {t.id === active && " ✓"}
   894→   894→            </button>
   895→   895→          ))}
   896→   896→        </div>
   897→   897→      )}
   898→   898→    </div>
   899→   899→  );
   900→   900→}
   901→   901→
   902→   902→interface SettingsMenuProps {
   903→   903→  speed: number;
   904→   904→  quality: string;
   905→   905→  onSpeed: (s: number) => void;
   906→   906→  onQuality: (q: string) => void;
   907→   907→  onHover: () => void;
   908→   908→  open: boolean;
   909→   909→  onOpenChange: (o: boolean) => void;
   910→   910→}
   911→   911→
   912→   912→function SettingsMenu({
   913→   913→  speed,
   914→   914→  quality,
   915→   915→  onSpeed,
   916→   916→  onQuality,
   917→   917→  onHover,
   918→   918→  open,
   919→   919→  onOpenChange,
   920→   920→}: SettingsMenuProps) {
   921→   921→  return (
   922→   922→    <div
   923→   923→      className="relative"
   924→   924→      onMouseEnter={onHover}
   925→   925→    >
   926→   926→      <button
   927→   927→        type="button"
   928→   928→        onClick={() => onOpenChange(!open)}
   929→   929→        className="grid h-8 w-8 place-items-center rounded-full hover:bg-white/10"
   930→   930→        aria-label="Settings"
   931→   931→      >
   932→   932→        <Settings className="h-4 w-4" />
   933→   933→      </button>
   934→   934→      {open && (
   935→   935→        <div className="absolute bottom-10 right-0 w-44 overflow-hidden rounded-md border border-white/10 bg-[#1a1a1f] shadow-xl">
   936→   936→          <div className="px-3 py-2">
   937→   937→            <p className="mb-1 text-[10px] font-semibold uppercase tracking-wide text-white/50">
   938→   938→              Speed
   939→   939→            </p>
   940→   940→            <div className="flex flex-wrap gap-1">
   941→   941→              {SPEEDS.map((s) => (
   942→   942→                <button
   943→   943→                  key={s}
   944→   944→                  type="button"
   945→   945→                  onClick={() => onSpeed(s)}
   946→   946→                  className={cn(
   947→   947→                    "rounded px-2 py-1 text-[11px] hover:bg-white/10",
   948→   948→                    s === speed
   949→   949→                      ? "bg-yellow-400 text-black"
   950→   950→                      : "text-white",
   951→   951→                  )}
   952→   952→                >
   953→   953→                  {s}×
   954→   954→                </button>
   955→   955→              ))}
   956→   956→            </div>
   957→   957→          </div>
   958→   958→          <div className="border-t border-white/10 px-3 py-2">
   959→   959→            <p className="mb-1 text-[10px] font-semibold uppercase tracking-wide text-white/50">
   960→   960→              Quality
   961→   961→            </p>
   962→   962→            <div className="flex flex-wrap gap-1">
   963→   963→              {QUALITIES.map((q) => (
   964→   964→                <button
   965→   965→                  key={q}
   966→   966→                  type="button"
   967→   967→                  onClick={() => onQuality(q)}
   968→   968→                  className={cn(
   969→   969→                    "rounded px-2 py-1 text-[11px] hover:bg-white/10",
   970→   970→                    q === quality
   971→   971→                      ? "bg-yellow-400 text-black"
   972→   972→                      : "text-white",
   973→   973→                  )}
   974→   974→                >
   975→   975→                  {q}
   976→   976→                </button>
   977→   977→              ))}
   978→   978→            </div>
   979→   979→          </div>
   980→   980→        </div>
   981→   981→      )}
   982→   982→    </div>
   983→   983→  );
   984→   984→}
   985→   985→