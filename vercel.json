{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "bun install && rm -rf node_modules/.prisma && prisma generate",
  "functions": {
    "src/app/api/stream/route.ts": {
      "maxDuration": 60
    }
  }
}
